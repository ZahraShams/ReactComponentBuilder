// call back which are implemented in the app so if these are mentioned in the JSON structure the app will respond by the request
export enum AllowedCallbacks {
  openModal = 'openModal',
  alert="alert"
}
